
/** @type {import('./$types').PageLoad} */
export async function load({ fetch, params }) {
    // const tokens = await fetch('/api/tokens').then(x => x.json());
    // return {
    //     tokens
    // };
}
